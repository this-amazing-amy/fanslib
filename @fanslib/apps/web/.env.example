# Electric --------------------
ELECTRIC_HOST=localhost
ELECTRIC_PORT=3001
ELECTRIC_TOKEN=""
ELECTRIC_URL=http://${ELECTRIC_HOST}:${ELECTRIC_PORT}

# Database --------------------
DATABASE_USER=postgres
DATABASE_PASSWORD=password
DATABASE_HOST=localhost
DATABASE_PORT=54321
DATABASE_DATABASE=electric
DATABASE_URL="postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_DATABASE}"

# Server ----------------------
# Backend API URL with protocol (required)
# Development: http://localhost:6970
# Production: https://api.example.com
VITE_API_URL=http://localhost:6970

# Media ------------------------------
MEDIA_ROOT="/app/content"
THUMBNAILS_ROOT="/app/thumbnails"
