{
  "name": "@fanslib/utils",
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "lint": "oxlint src/ && eslint src/",
    "typecheck": "tsc",
    "format": "oxfmt --write src/",
    "format:check": "oxfmt --check src/"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
